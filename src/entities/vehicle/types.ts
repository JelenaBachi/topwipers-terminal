export type Brand = { code: string; name: string };
export type Model = { code: string; name: string };
export type Mod = { code: string; name: string };

export type BrandList = Brand[];
export type ModelList = { models: Model[] };
export type ModList = { mods: Mod[] };
